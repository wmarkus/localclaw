---
summary: "OAuth is not used for local-only models"
read_when:
  - You are looking for provider OAuth details
title: "OAuth"
---

# OAuth

Local-only builds do **not** use OAuth for model providers.

Some messaging channels may still have their own OAuth or token setup, but model
inference itself is local and does not require OAuth.
